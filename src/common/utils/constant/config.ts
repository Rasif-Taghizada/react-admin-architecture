const SIDEBAR_WIDTH = 230;
const SIDEBAR_COLLAPSED_WIDTH = 80;

//button start
const VARIANTS = {
  primary: "btn btn-primary",
  secondary: "btn btn-secondary",
  outline: "btn btn-outline",
  ghost: "btn btn-ghost",
  danger: "btn btn-danger",
} as const;

const SIZES = {
  md: "btn-md",
  sm: "btn-sm",
  lg: "btn-lg",
} as const;
//button end

export { SIDEBAR_WIDTH, SIDEBAR_COLLAPSED_WIDTH, VARIANTS, SIZES };